API（Application Programming Interface，应用程序接口）是一些预先定义的接口（如函数、HTTP接口），或指软件系统不同组成部分衔接的约定。用来提供应用程序与开发人员基于某软件或硬件得以访问的一组例程，而又无需访问源码，或理解内部工作机制的细节。

两种含义：
1.封装好的函数(语法之类的)

2.前后端交互的结果：(这四个信息就是借口的组成部分)
1).请求地址  /login
2).请求方式  /post
3).参数类型  /json/urlencoded
4).相应字体格式 /utf-8
